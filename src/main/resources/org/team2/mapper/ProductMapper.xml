<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.team2.mapper.ProductMapper">
    <select id="getOne" resultType="org.team2.domain.ProductVO">
        select *
        from tbl_product
        where product_seq = #{seq}
    </select>


    <select id="getUserOrder" resultType="org.team2.domain.ProductVO">
        select tbl_product.product_seq ,tbl_product.product_code, tbl_product.product_name, tbl_product.product_cost
        from tbl_product, tbl_op, tbl_order
        where tbl_product.product_seq = tbl_op.product_seq
          and tbl_op.order_seq = tbl_order.order_seq
          and tbl_order.user_seq=#{seq}
    </select>

    <select id="getAll" resultType="map">
        select *
        from tbl_product
    </select>

    <select id="getMain" resultType="map">
        <![CDATA[
        SELECT *
        FROM (SELECT * FROM tbl_product)
        WHERE ROWNUM <= 10
        ]]>
    </select>

    <insert id="insert">
        INSERT INTO tbl_product (product_seq,
                                 product_code,
                                 product_name,
                                 product_info,
                                 product_detail,
                                 product_brand,
                                 product_manufacture,
                                 product_country,
                                 product_count,
                                 op2_seq1,
                                 op2_seq2,
                                 op2_seq3,
                                 op2_seq4,
                                 op2_seq5,
                                 category_seq,
                                 created_at,
                                 updated_at)
        VALUES (seq_product.NEXTVAL,
                #{product_code},
                #{product_name},
                #{product_info},
                #{product_detail},
                #{product_brand},
                #{product_manufacture},
                #{product_country},
                #{product_count},
                #{op2_seq1, jdbcType=VARCHAR},
                #{op2_seq2, jdbcType=VARCHAR},
                #{op2_seq3, jdbcType=VARCHAR},
                #{op2_seq4, jdbcType=VARCHAR},
                #{op2_seq5, jdbcType=VARCHAR},
                #{category_seq},
                sysdate,
                sysdate)
    </insert>

    <update id="update">
        UPDATE tbl_product
        SET PRODUCT_NAME        = #{product_name},
            PRODUCT_INFO        = #{product_info},
            PRODUCT_DETAIL      = #{product_detail},
            PRODUCT_BRAND       = #{product_brand},
            PRODUCT_MANUFACTURE = #{product_manufacture},
            PRODUCT_COUNTRY     = #{product_country},
            PRODUCT_COUNT       = #{product_count},
            OP2_SEQ1            = #{op2_seq1, jdbcType=VARCHAR},
            OP2_SEQ2            = #{op2_seq2, jdbcType=VARCHAR},
            OP2_SEQ3            = #{op2_seq3, jdbcType=VARCHAR},
            OP2_SEQ4            = #{op2_seq4, jdbcType=VARCHAR},
            OP2_SEQ5            = #{op2_seq5, jdbcType=VARCHAR},
            CATEGORY_SEQ        = #{category_seq},
            updated_at          = SYSDATE
        WHERE PRODUCT_SEQ = #{seq}
    </update>

    <delete id="delete">
        DELETE
        FROM tbl_product p
        WHERE p.product_seq = #{product_seq}
    </delete>
</mapper>