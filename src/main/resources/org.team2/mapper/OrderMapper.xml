<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.team2.mapper.OrderMapper">

    <insert id="insert">
        INSERT INTO tbl_order(order_seq,
                              user_seq,
                              order_point,
                              order_status,
                              order_user_name,
                              order_user_number,
                              order_delivery,
                              order_invoice,
                              created_at,
                              updated_at)
        values (seq_order.nextval,
                #{userSeq},
                #{point},
                #{status},
                #{userName},
                #{userNumber},
                #{delivery},
                #{invoice},
                sysdate,
                sysdate)
    </insert>

</mapper>